'use client';

// Este arquivo foi descontinuado em favor de ProductListItem.tsx
// Mantido apenas para compatibilidade de build

import { Product } from '../types/product';

interface MenuItemProps {
  product: Product;
}

export default function MenuItem({ product }: MenuItemProps) {
  return null;
}
